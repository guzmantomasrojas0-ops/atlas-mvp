/**
 * Ventana horaria fija que muestra el calendario (8:00–20:00). Todavía no
 * existe un módulo de "horario del negocio" — cuando exista, esto se
 * reemplaza por el horario real configurado, no por esta constante.
 */
export const BUSINESS_HOURS_START = 8;
export const BUSINESS_HOURS_END = 20;
export const SLOT_MINUTES = 30;
