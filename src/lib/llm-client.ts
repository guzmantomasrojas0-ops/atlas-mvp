// Adaptador del proveedor LLM.
// Fase 0: no se implementa IA todavía (ver PLAN.md, Fase 3 en adelante).
// Este archivo marca el punto donde se abstraerá el proveedor (ej. Anthropic)
// detrás de una interfaz, para no acoplar el resto del sistema a un SDK específico.
export {};
